# =============================================
# 🎯 SportSync AI - Environment Configuration
# =============================================
# 
# SETUP GUIDE:
# 1. Copy this file: cp .env.example .env
# 2. Choose ONE API provider (Groq recommended for free tier)
# 3. Get your API key and paste it below
# 4. Save and run: streamlit run app_streamlit.py
#
# دليل الإعداد:
# 1. انسخ هذا الملف: cp .env.example .env
# 2. اختر مزود واحد (Groq مجاني ومُوصى به)
# 3. احصل على مفتاح API وضعه أدناه
# 4. احفظ وشغّل: streamlit run app_streamlit.py
# =============================================

# ============================================
# 🔑 LLM API KEYS (Choose ONE)
# ============================================

# ┌─────────────────────────────────────────┐
# │ OPTION 1: Groq (FREE & RECOMMENDED)     │
# │ ✅ Fast (sub-second responses)          │
# │ ✅ Free tier: 6000 req/min             │
# │ ✅ Great for development                │
# │ 🔗 Get key: https://console.groq.com    │
# └─────────────────────────────────────────┘
GROQ_API_KEY=

# ┌─────────────────────────────────────────┐
# │ OPTION 2: OpenAI (PAID, HIGH QUALITY)   │
# │ ✅ Best reasoning (GPT-4, o1-mini)      │
# │ ⚠️ Requires payment method              │
# │ 💰 ~$0.01 per recommendation            │
# │ 🔗 Get key: https://platform.openai.com │
# └─────────────────────────────────────────┘
OPENAI_API_KEY=

# ┌─────────────────────────────────────────┐
# │ OPTION 3: OpenRouter (FLEXIBLE)         │
# │ ✅ Access to 100+ models                │
# │ ✅ Pay per use                          │
# │ ✅ Free models available                │
# │ 🔗 Get key: https://openrouter.ai       │
# └─────────────────────────────────────────┘
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_APP_TITLE=SportSync_AI
OPENROUTER_REFERRER=https://sportsync.ai

# ============================================
# 🤖 MODEL CONFIGURATION
# ============================================

# Main Model (used for recommendations)
# النموذج الرئيسي (للتوصيات)
#
# For Groq (free):
CHAT_MODEL=llama-3.1-70b-versatile
#
# For OpenAI:
# CHAT_MODEL=gpt-4o-mini
# CHAT_MODEL=gpt-4o
# CHAT_MODEL=o1-mini
#
# For OpenRouter:
# CHAT_MODEL=meta-llama/llama-3.1-70b-instruct
# CHAT_MODEL=anthropic/claude-3.5-sonnet

# Fallback Model (if main model fails)
# نموذج احتياطي (في حالة فشل النموذج الرئيسي)
CHAT_MODEL_FALLBACK=llama-3.1-8b-instant

# Multiple models (comma-separated for rotation)
# نماذج متعددة (مفصولة بفواصل للتبديل التلقائي)
# CHAT_MODEL=llama-3.1-70b,gpt-4o-mini,claude-3-sonnet

# ============================================
# ⚙️ SYSTEM CONFIGURATION
# ============================================

# Enable KB-Only Mode (works without API key)
# تفعيل وضع قاعدة المعرفة فقط (يعمل بدون مفتاح API)
ENABLE_KB_FALLBACK=true

# LLM Settings
# إعدادات نموذج اللغة
LLM_TIMEOUT_SECONDS=30
LLM_MAX_RETRIES=3
LLM_SEED=42

# Logging
# السجلات
LOG_LEVEL=INFO
LLM_INIT_LOG=1

# ============================================
# 🎬 OPTIONAL: VIDEO GENERATION
# ============================================

# Stock Images APIs (for video backgrounds)
# واجهات برمجة الصور (لخلفيات الفيديو)
PEXELS_API_KEY=
UNSPLASH_ACCESS_KEY=
PIXABAY_API_KEY=

# AI Image Generation (RunPod + Stable Diffusion)
# توليد صور بالذكاء الاصطناعي
RUNPOD_API_KEY=
RUNPOD_ENDPOINT_ID=

# ============================================
# 🗄️ OPTIONAL: DATABASE (User Accounts)
# ============================================

# Supabase (for user profiles, progress tracking, feedback)
# Supabase (لحسابات المستخدمين، تتبع التقدم، الملاحظات)
SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_SERVICE_KEY=

# ============================================
# 🎛️ FEATURE FLAGS
# ============================================

# Enable/Disable Features
# تفعيل/تعطيل الميزات
ENABLE_DYNAMIC_CHAT=true
ENABLE_VIDEO_GENERATION=false
ENABLE_USER_ACCOUNTS=false
ENABLE_KB_RANKER=true
ENABLE_LAYER_Z=true
ENABLE_COLLABORATIVE_FILTERING=false

# ============================================
# 📊 OPTIONAL: ANALYTICS
# ============================================

# Analytics & Tracking (optional)
# التحليلات والتتبع (اختياري)
MIXPANEL_TOKEN=
GOOGLE_ANALYTICS_ID=

# ============================================
# 🌍 APPLICATION SETTINGS
# ============================================

APP_VERSION=2.3.0
APP_ENV=development
APP_URL=http://localhost:8501

# ============================================
# ⚠️ TROUBLESHOOTING TIPS
# ============================================
#
# 1. "No LLM client" error:
#    → Add GROQ_API_KEY or OPENAI_API_KEY above
#    → Or set ENABLE_KB_FALLBACK=true (works offline)
#
# 2. "Authentication failed" error:
#    → Check your API key is correct
#    → Groq keys start with: gsk_
#    → OpenAI keys start with: sk-proj- or sk-
#    → No spaces or quotes around the key
#
# 3. "Rate limit exceeded":
#    → Wait 60 seconds
#    → Or switch to a different model
#    → Or enable KB_FALLBACK mode
#
# 4. Slow responses:
#    → Use faster models (llama-3.1-8b-instant)
#    → Reduce LLM_TIMEOUT_SECONDS
#    → Enable caching
#
# 5. Need help?
#    → Check README.md
#    → Open issue on GitHub
#    → Email: support@sportsync.ai
# ============================================